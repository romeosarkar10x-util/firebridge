function Footer() {
    return (
        <footer className="py-4 text-center text-xs text-muted-foreground">
            FireBridge does not store your credentials. All data is cleared on sign out.
        </footer>
    );
}

export { Footer };
